import 'package:flutter/material.dart';

class TabActivities extends StatelessWidget {
  const TabActivities({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Activités / Présence (pointage + rapports) — à brancher API"),
    );
  }
}
